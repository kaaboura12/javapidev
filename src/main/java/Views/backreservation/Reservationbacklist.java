package Views.backreservation;

import Controllers.backevent.ReservationBackList;

public class Reservationbacklist extends ReservationBackList {
    // This class is needed for compatibility with the FXML controller lookup
    // It extends the actual controller implementation which is in the Controllers package
} 